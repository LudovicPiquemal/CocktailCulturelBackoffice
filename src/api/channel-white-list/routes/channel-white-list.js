'use strict';

/**
 * channel-white-list router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::channel-white-list.channel-white-list');
