'use strict';

/**
 * channel-black-list service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::channel-black-list.channel-black-list');
